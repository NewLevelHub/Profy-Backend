server.sh
start.sh
.env
.env.local
.env.production
docker-compose.local.yml
nginx.conf
nginx.local.conf
nginx/ssl/*.pem
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.log

# University photos — big binary asset, distributed as profy-media.tar.gz out of band
media/
profy-media/
*.tar.gz

# KZ-504: Kazakh translation working directories (not committed, generated per-session)
scripts/data/kk_todo/
scripts/data/kk_done/

# Scratch files for the kk translation passes (KZ-504 / requirements dictionary):
# the todo is regenerated by each script's `dump` and folded in by `merge`;
# only the merged store under app/i18n/data/ is committed.
scripts/data/*_kk.todo.json
